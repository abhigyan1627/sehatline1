{
  "name": "sehatline-frontend",
  "version": "1.0.0",
  "description": "SehatLine static frontend for clinic discovery and SmartQueue.",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "start": "vite --host 127.0.0.1 --port 3000",
    "preview": "vite preview --host 127.0.0.1 --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
